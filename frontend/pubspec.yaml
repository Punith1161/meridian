name: meridian
description: MERIDIAN — Manage Every Responsibility Integrated Daily In Aligned Notes.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^17.2.3
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  flutter_markdown_plus: ^1.0.7
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
